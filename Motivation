for _ in range(int(input())):
    n,x=map(int,input().split())
    max_rating=0
    for i in range(n):
        s,r=map(int,input().split())
        if s<=x and r>=max_rating:
            max_rating=r
    print(max_rating)
